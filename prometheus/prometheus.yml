global:
  scrape_interval: 5s      # scrape every 15 seconds

scrape_configs:
  - job_name: 'battery-monitor'
    static_configs:
      - targets: ['host.docker.internal:9090']
        # host.docker.internal = your laptop from inside Docker